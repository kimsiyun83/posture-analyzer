"use client";
import {useState} from "react";
export default function Settings({signupEnabled,cameraEnabled}:{signupEnabled:boolean;cameraEnabled:boolean}){const [message,setMessage]=useState("");return <form className="care-card portal-form" onSubmit={async e=>{e.preventDefault();const f=new FormData(e.currentTarget);try{const r=await fetch("/api/admin/customer-settings",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({signupEnabled:f.has("signup"),cameraEnabled:f.has("camera")})});setMessage(r.ok?"설정을 저장했습니다.":"저장하지 못했습니다.");}catch{setMessage("연결을 확인해 주세요.");}}}><h2>관리자 설정 모드</h2><label><input type="checkbox" name="signup" defaultChecked={signupEnabled}/> 신규 고객 가입 허용</label><label><input type="checkbox" name="camera" defaultChecked={cameraEnabled}/> 실시간 카메라 기능검사 허용</label><p>검사 저장은 로그인한 고객만 가능합니다. 영상은 보관하지 않습니다.</p><button className="care-primary">설정 저장</button><p role="status">{message}</p></form>;}
